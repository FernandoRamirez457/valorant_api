import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  imgBanner: {
    width: "93%",
    height: 200,
    marginTop: 15,
    borderRadius: 2,
  },
});

export default styles;
